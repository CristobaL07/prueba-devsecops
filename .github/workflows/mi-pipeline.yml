name: Pipeline JavaScript
on:
  push:
    branches: [main]
jobs:
  testear-codigo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Ejecutar test automatico
        run: node test.js
